rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /inscricoes/{inscricaoId} {
      allow read: if true;
      allow write: if true; // WARNING: This rule is insecure and should be restricted in production.
    }
  }
}